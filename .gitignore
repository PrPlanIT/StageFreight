.stagefreight/*
!.stagefreight/badges/
src/output/banner_art_gen.go

# compiled binaries — never commit these
/cli
/stagefreight
/stagefreight-bin
*.exe

# binary build output (dist/<os>-<arch>/ binaries, archives, SHA256SUMS)
/dist/
